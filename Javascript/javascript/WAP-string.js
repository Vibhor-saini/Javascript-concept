// WAP to guide user . for exp: = username should be @vibhor


// let name = prompt("Enter your Name: ");
// let username = "@" + name + name.length;
// document.write(username);

// while(name[0]!="@"){
//    name =  prompt("Name should start with @ and sholuld be like : " + "@"+name );
// }

// document.write("Your name is: " + name + name.length);

// function capitalizeFirstLetter(z) {
//     return z.slice(0, 1).toUpperCase() + z.slice(1);
// }

// let result = capitalizeFirstLetter("hello");
// const mySentence = "freeCodeCamp is an awesome resource";

// const result = mySentence.replace(/\b\w/g, (match) => match.toUpperCase());

// console.log(result);


// const mySentence = "freeCodeCamp is an awesome resource";
// const words = mySentence.split(" ");

// words.map((word) => { 
//     return word[0].toUpperCase() + word.substring(1); 
// }).join(" ");


// const mySentence = "freeCodeCamp is an awesome resource";

// const result = mySentence.replace(/\b\w/g, (match) => match.toUpperCase());

// document.write(result);

